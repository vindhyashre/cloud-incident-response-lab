global:
  scrape_interval: 15s

rule_files:
  - "rules/alerts.yml"

scrape_configs:

  - job_name: "application"
    static_configs:
      - targets:
          - "app:5000"

  - job_name: "node"
    static_configs:
      - targets:
          - "node-exporter:9100"